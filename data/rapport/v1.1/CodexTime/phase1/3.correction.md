# Etape 3 - Prompt de correction (micro-passe robustesse)

Objectif:
Appliquer une micro-passe locale de robustesse technique sur `CodexWorkflowClient` sans élargir le périmètre.

Corrections autorisées:
1. Introduire une constante locale pour le binaire Codex (`CODEX_BINARY`).
2. Ajuster minimalement le délai de collecte stdout/stderr pour réduire le risque de sortie tronquée.
3. Ajouter une validation défensive simple de `workingDirectory` (existence + type répertoire).

Contraintes:
- pas de refactor
- pas de configuration externe
- pas de nouvelle fonctionnalité
- pas de logique workflow
