Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 2 — Étape 2

Objectif de l’étape :
analyser l’introduction de la première vraie step métier du workflow :
`GlobalAnalysisStep`

Important :
dans CE prompt, je demande une **analyse uniquement**.

## Règle absolue

### Analyse uniquement — ne pas modifier le code

Tu dois :

* analyser l’existant
* proposer le design
* identifier les classes à créer ou modifier
* signaler les risques
* proposer un plan d’implémentation sobre

Tu ne dois PAS :

* créer de fichier
* modifier du code
* implémenter quoi que ce soit
* lancer une passe de refactor
* prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

---

## 0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase2/`

Nommage attendu pour cette étape :

* prompt d’analyse :
  `2.analysis.md`
* résultat d’analyse :
  `result.2.analysis.md`

Important :

* enregistrer le prompt reçu dans `2.analysis.md`
* enregistrer le rapport d’analyse dans `result.2.analysis.md`

Ne pas produire de fichier `2.implements.md` ni `result.2.implements.md` à ce stade, puisque cette phase est une analyse uniquement.

---

## 1. Contexte existant

Les briques suivantes existent déjà :

* runtime minimal :

  * `WorkflowRun`
  * `LotRun`
  * `WorkflowExecutionContext`
  * statuts associés
* contrat d’étape :

  * `WorkflowStep`
  * `WorkflowStepDecision`
  * `WorkflowStepResult`
* client technique Codex :

  * `CodexExecutionRequest`
  * `CodexExecutionResult`
  * `CodexWorkflowClient`
  * `CodexExecutionException`
* service de gestion des artefacts :

  * `WorkflowArtifactType`
  * `WorkflowArtifactService`
* orchestrator minimal :

  * `WorkflowOrchestrator`

Nous voulons maintenant introduire la première vraie step métier.

---

## 2. Objectif de l’analyse

Je veux que tu analyses comment introduire proprement une step du type :

* `GlobalAnalysisStep`

Cette step devra plus tard :

* implémenter `WorkflowStep`
* lire les informations nécessaires dans `WorkflowExecutionContext`
* construire un prompt d’analyse globale
* appeler `CodexWorkflowClient`
* écrire les artefacts documentaires via `WorkflowArtifactService`
* retourner un `WorkflowStepResult`

Mais dans CE prompt :

* tu ne dois PAS l’implémenter
* tu dois seulement analyser la meilleure manière simple de le faire

---

## 3. Ce que cette future step devra faire

Au moment de l’implémentation, cette future step devra être capable de :

1. lire dans le contexte les informations utiles à l’analyse globale
2. construire le texte du prompt d’analyse globale
3. exécuter Codex via le client dédié
4. écrire :

   * le prompt d’analyse
   * le résultat d’analyse
5. retourner un `WorkflowStepResult`

Important :
cette step ne doit pas devenir un orchestrator.
Elle exécute une responsabilité métier précise, puis retourne un résultat.

---

## 4. Points à analyser explicitement

### A. Placement

Propose le package le plus cohérent pour cette première step métier.

### B. Responsabilité exacte

Définis précisément ce que fait `GlobalAnalysisStep`, et ce qu’elle ne doit surtout pas faire.

### C. Dépendances minimales

Indique quelles briques existantes elle doit utiliser directement :

* `WorkflowExecutionContext`
* `CodexWorkflowClient`
* `WorkflowArtifactService`
* éventuellement `WorkflowArtifactType`

Et indique aussi clairement ce qu’elle ne doit pas absorber.

### D. Rapport au contexte runtime

Analyse quelles données minimales doivent déjà être présentes dans `WorkflowExecutionContext` pour permettre à cette step de fonctionner.

Important :
si le contexte actuel ne suffit pas, signale-le explicitement.
Mais ne propose pas d’usine à gaz.

### E. Rapport aux artefacts

Analyse comment la step doit demander l’écriture :

* du prompt d’analyse
* du résultat d’analyse

sans disperser la logique documentaire partout.

### F. Rapport à Codex

Analyse comment la step doit appeler `CodexWorkflowClient` proprement :

* quel niveau de responsabilité lui revient
* ce qui doit rester dans le client Codex
* ce qui doit rester hors de la step

### G. Résultat de la step

Analyse quel `WorkflowStepResult` cette step doit raisonnablement retourner dans cette première version.

Je veux une recommandation simple et concrète.

### H. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

* boucle de correction
* multi-step
* parsing intelligent de roadmap
* décisions humaines avancées
* validation Maven
* Telegram
* logique d’orchestration globale

---

## 5. Question importante à traiter

Je veux que tu répondes explicitement à ce point :

### Le contexte runtime actuel contient-il déjà assez d’informations pour permettre à `GlobalAnalysisStep` de fonctionner proprement ?

Par exemple, cette step aura probablement besoin de connaître au minimum :

* où écrire les artefacts
* quel numéro d’étape traiter
* éventuellement quelle phase/version utiliser
* éventuellement quel contenu documentaire source exploiter

Donc dans ton analyse, indique clairement l’une des conclusions suivantes :

1. le contexte actuel suffit tel quel
2. le contexte actuel ne suffit pas et il faut ajouter un minimum d’informations
3. le contexte actuel suffit si certaines conventions simples sont imposées

Je veux une réponse nette sur ce point.

---

## 6. Contraintes fortes

Je veux une solution :

* simple
* lisible
* progressive
* sans framework caché
* sans enrichissement prématuré du runtime
* sans mini moteur de prompt

Je ne veux pas :

* de parser sémantique complexe
* de moteur de templating avancé
* de stratégie multi-step
* d’abstraction inutile
* d’intégration Telegram
* de validation Maven
* de logique de review/correction dans cette step

---

## 7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. le package recommandé
2. les classes à créer ou modifier
3. la responsabilité exacte de `GlobalAnalysisStep`
4. les dépendances minimales
5. le besoin éventuel d’ajustement du contexte runtime
6. les risques de sur-conception
7. le plan d’implémentation minimal
8. ce qui devra rester volontairement absent

---

## 8. Point de vigilance majeur

Le principal risque de cette étape est de transformer `GlobalAnalysisStep` en :

* mini orchestrator
* mini moteur de prompting
* ou point d’entrée générique de tout le workflow

Je veux au contraire :

* une step métier unique
* claire
* bornée
* qui fait une seule chose

Tu dois rester strictement dans cette analyse.