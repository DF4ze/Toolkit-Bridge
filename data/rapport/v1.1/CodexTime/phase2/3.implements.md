Tu peux passer à l’implémentation.

Ton analyse est globalement validée.

Décisions retenues :

* point d’entrée recommandé : **test d’intégration local dédié**
* construction d’un `WorkflowExecutionContext` minimal explicite
* instanciation directe des dépendances, sans framework supplémentaire
* exécution réelle via `WorkflowOrchestrator + GlobalAnalysisStep`
* validation par décision retournée + artefacts générés

## IMPORTANT

### Implémentation uniquement

Dans ce prompt, tu peux implémenter.

Mais :

* pas de refactor hors périmètre
* pas de nouvelle abstraction
* pas d’API REST
* pas de Telegram
* pas de Spring supplémentaire
* pas de main si le test dédié suffit
* pas de runner générique multi-workflows

---

## 0. Règles de traçabilité documentaire

Les artefacts documentaires de CETTE étape doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase2/`

Nommage attendu pour cette étape :

* prompt d’implémentation :
  `3.implements.md`
* résultat d’implémentation :
  `result.3.implements.md`
* prompt de review :
  `3.review.md`
* résultat de review :
  `result.3.review.md`
* prompt de correction si nécessaire :
  `3.correction.md`
* résultat de correction si nécessaire :
  `result.3.correction.md`

Important :

* enregistrer aussi le prompt d’implémentation dans `3.implements.md`
* enregistrer le rapport d’implémentation dans `result.3.implements.md`
* si tu fais une auto-relecture critique, l’enregistrer dans `result.3.review.md`

Garder impérativement la structure :

* `[numEtape].XXX.md`
* `result.[numEtape].XXX.md`

Important aussi :
le runner exécuté devra générer les artefacts métier de `GlobalAnalysisStep`, à savoir :

* `3.analysis.md`
* `result.3.analysis.md`

Il ne faut pas les confondre avec les artefacts documentaires de ce lot d’implémentation.

---

## 1. Objectif précis

Implémenter un runner minimal permettant d’exécuter réellement le workflow localement, de bout en bout, en branchant :

* `WorkflowOrchestrator`
* `GlobalAnalysisStep`

Je veux un point d’entrée concret, testable, simple, permettant de lancer le workflow et d’observer les artefacts générés.

---

## 2. Forme retenue

Implémenter un **test d’intégration local dédié**, par exemple du type :

* `GlobalAnalysisWorkflowRunnerTest`

Important :
ce test dépend potentiellement de la présence locale de `codex`.

Donc je veux un test :

* clairement identifié comme test d’intégration locale
* conçu pour être lancé explicitement
* sans fragiliser inutilement la suite de tests standard

Reste sobre dans la manière de faire.
Je ne veux pas de framework de test complexe ni de système de profiles sophistiqué pour ce lot.

---

## 3. Construction du contexte

Construire un `WorkflowExecutionContext` minimal avec :

* un `WorkflowRun` valide
* `lotRun = null` si cela suffit pour ce lot
* `variables = Map<String,Object>` avec les clés minimales requises par `GlobalAnalysisStep`

Variables obligatoires à initialiser :

* `reportRootDirectory`
* `reportVersion`
* `reportPhase`
* `stepNumber`
* `analysisSourcePath`

Variables optionnelles utiles :

* `codexWorkingDirectory`
* `codexTimeoutSeconds`

Tu peux utiliser des valeurs cohérentes avec le projet local, dans l’esprit de l’analyse validée.

---

## 4. Instanciation des dépendances

Instanciation directe, sans couche supplémentaire :

* `WorkflowArtifactService`
* `CodexWorkflowClient`
* `GlobalAnalysisStep`
* `WorkflowOrchestrator`

Pas d’injection Spring nécessaire pour ce lot si elle n’apporte rien.

---

## 5. Exécution attendue

Le runner doit exécuter :

* `WorkflowStepResult result = orchestrator.executeSingleStep(context, step);`

Puis vérifier au minimum :

### En cas nominal

* décision `CONTINUE`
* existence de :

  * `3.analysis.md`
  * `result.3.analysis.md`
* contenu non vide ou minimalement cohérent des artefacts
* présence dans `result.data()` de :

  * `promptArtifactPath`
  * `resultArtifactPath`

Important :
ne pas sur-spécifier le texte produit par Codex.
Je veux des assertions robustes et sobres, pas des vérifications fragiles du contenu exact.

---

## 6. Contraintes de conception

Je veux une implémentation :

* simple
* locale
* testable
* sans abstraction inutile
* sans généralisation prématurée

Je ne veux pas :

* de runner générique
* de boucle
* de multi-step
* de retry
* de Telegram
* de Maven
* de parsing de roadmap
* de refactor des classes existantes sans nécessité réelle

---

## 7. Ce que je ne veux pas dans ce lot

* pas de modification d’architecture
* pas d’enrichissement du runtime
* pas de changement de contrat de `GlobalAnalysisStep`
* pas de changement de contrat de `WorkflowOrchestrator`
* pas de nouvelle couche applicative
* pas de main si le test dédié suffit

---

## 8. Travail demandé

1. implémenter le test d’intégration local dédié
2. construire le contexte minimal
3. instancier les dépendances réelles
4. exécuter le workflow de bout en bout
5. vérifier le résultat retourné et les artefacts générés
6. produire une auto-relecture critique
7. expliciter dans le rapport :

   * ce qui a été implémenté
   * ce qui n’a volontairement pas été implémenté
   * les points à surveiller pour la suite

---

## 9. Point de vigilance majeur

Le principal risque de ce lot est de construire un système de lancement trop ambitieux.

Je veux au contraire :

* un point d’entrée minimal
* concret
* local
* utile immédiatement

Le but est simplement :
**faire tourner réellement le workflow et voir les artefacts générés**.

Tu peux maintenant implémenter ce lot dans ce périmètre exact.