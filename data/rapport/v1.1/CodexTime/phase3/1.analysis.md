Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 3 — Étape 1

Objectif de l’étape :
analyser l’introduction de la deuxième vraie step métier du workflow :
`GlobalReviewStep`

Important :
dans CE prompt, je demande une **analyse uniquement**.

## Règle absolue

### Analyse uniquement — ne pas modifier le code

Tu dois :

* analyser l’existant
* proposer le design
* identifier les classes à créer ou modifier
* signaler les risques
* proposer un plan d’implémentation sobre

Tu ne dois PAS :

* créer de fichier
* modifier du code
* implémenter quoi que ce soit
* lancer une passe de refactor
* prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

---

## 0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase3/`

Nommage attendu pour cette étape :

* prompt d’analyse :
  `1.analysis.md`
* résultat d’analyse :
  `result.1.analysis.md`

Important :

* enregistrer le prompt reçu dans `1.analysis.md`
* enregistrer le rapport d’analyse dans `result.1.analysis.md`

Ne pas produire de fichier `1.implements.md` ni `result.1.implements.md` à ce stade.

---

## 1. Contexte existant

Les briques suivantes existent déjà :

* `WorkflowOrchestrator`
* `GlobalAnalysisStep`
* `WorkflowArtifactService`
* `WorkflowArtifactType`
* `CodexWorkflowClient`
* `WorkflowExecutionContext`
* `WorkflowStep`
* `WorkflowStepResult`

Le workflow minimal actuel sait :

* exécuter une step
* lancer `GlobalAnalysisStep`
* produire :

  * `ANALYSIS_PROMPT`
  * `ANALYSIS_RESULT`

Nous voulons maintenant introduire une deuxième step métier :

* `GlobalReviewStep`

---

## 2. Objectif de l’analyse

Je veux que tu analyses comment introduire proprement une step du type :

* `GlobalReviewStep`

Cette step devra plus tard :

* implémenter `WorkflowStep`
* relire les artefacts produits par `GlobalAnalysisStep`
* construire un prompt de review simple
* appeler `CodexWorkflowClient`
* écrire :

  * `REVIEW_PROMPT`
  * `REVIEW_RESULT`
* retourner un `WorkflowStepResult`

Mais dans CE prompt :

* tu ne dois PAS l’implémenter
* tu dois seulement analyser la meilleure manière simple de le faire

---

## 3. Ce que cette future step devra faire

Au moment de l’implémentation, cette future step devra être capable de :

1. retrouver les artefacts d’analyse déjà générés
2. lire au minimum :

   * le prompt d’analyse
   * le résultat d’analyse
3. construire un prompt de review simple
4. exécuter Codex via le client dédié
5. écrire :

   * le prompt de review
   * le résultat de review
6. retourner un `WorkflowStepResult`

Important :
cette step ne doit pas devenir :

* un orchestrator
* un moteur de correction
* un routeur
* un système de décision globale

---

## 4. Points à analyser explicitement

### A. Placement

Propose le package le plus cohérent pour cette step.

### B. Responsabilité exacte

Définis précisément ce que fait `GlobalReviewStep`, et ce qu’elle ne doit surtout pas faire.

### C. Dépendances minimales

Indique quelles briques existantes elle doit utiliser directement :

* `WorkflowExecutionContext`
* `WorkflowArtifactService`
* `WorkflowArtifactType`
* `CodexWorkflowClient`

Et indique clairement ce qu’elle ne doit pas absorber.

### D. Rapport aux artefacts existants

Analyse comment la step doit retrouver les artefacts générés par `GlobalAnalysisStep` sans dupliquer la logique de nommage.

### E. Rapport au contexte runtime

Analyse quelles données minimales doivent être disponibles dans `WorkflowExecutionContext` pour que `GlobalReviewStep` fonctionne proprement.

Je veux une réponse nette :

1. le contexte actuel suffit
2. le contexte actuel ne suffit pas
3. le contexte actuel suffit avec conventions simples

### F. Rapport à Codex

Analyse comment utiliser `CodexWorkflowClient` sans faire dériver la step vers un rôle d’orchestrator.

### G. Résultat de la step

Analyse quel `WorkflowStepResult` cette step doit raisonnablement retourner dans une première version.

### H. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

* correction automatique
* retry
* boucle
* multi-step avancé
* Telegram
* Maven
* décisions humaines avancées
* parsing sémantique lourd

---

## 5. Question importante

Je veux que tu répondes explicitement à ceci :

### `GlobalReviewStep` doit-elle lire les chemins des artefacts précédents depuis `WorkflowStepResult.data`, ou doit-elle simplement les reconstruire via `WorkflowArtifactService` à partir des mêmes conventions de contexte ?

Je veux une recommandation nette et argumentée.

Le but est de choisir une approche simple, stable et peu couplée.

---

## 6. Contraintes fortes

Je veux une solution :

* simple
* lisible
* progressive
* sans framework caché
* sans moteur de review
* sans généralisation prématurée

Je ne veux pas :

* de correction automatique
* de classification sophistiquée
* de parser de rapport avancé
* de logique métier globale
* de couplage Telegram
* de validation Maven

---

## 7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. le package recommandé
2. les classes à créer ou modifier
3. la responsabilité exacte de `GlobalReviewStep`
4. les dépendances minimales
5. le besoin éventuel d’ajustement du contexte runtime
6. le choix recommandé pour retrouver les artefacts précédents
7. les risques de sur-conception
8. le plan d’implémentation minimal
9. ce qui devra rester volontairement absent

---

## 8. Point de vigilance majeur

Le principal risque de cette étape est de transformer `GlobalReviewStep` en :

* mini orchestrator
* mini moteur de correction
* ou analyseur générique de tout le workflow

Je veux au contraire :

* une step métier unique
* claire
* bornée
* qui relit l’analyse précédente et produit une review simple

Tu dois rester strictement dans cette analyse.
