Tu peux passer à l’implémentation.

Ton analyse est validée.

## Objectif

Implémenter `GlobalReviewStep` de manière simple et strictement conforme à l’analyse.

---

## À implémenter

* `GlobalReviewStep implements WorkflowStep`
* `GlobalReviewStepTest`

---

## Comportement attendu

La step doit :

1. Lire les variables obligatoires dans `WorkflowExecutionContext`
2. Construire les chemins via `WorkflowArtifactService`
3. Lire :

   * `ANALYSIS_PROMPT`
   * `ANALYSIS_RESULT`

### IMPORTANT

Si un des fichiers est absent :
→ `STOP_FAILURE`

---

4. Construire un prompt de review simple (texte brut, sans logique complexe)

5. Appeler `CodexWorkflowClient`

6. Écrire :

   * `REVIEW_PROMPT`
   * `REVIEW_RESULT`

7. Retourner un `WorkflowStepResult`

---

## Résultat attendu

### Succès

* decision = `CONTINUE`
* message simple
* data :

  * `promptArtifactPath`
  * `resultArtifactPath`

### Échec

* decision = `STOP_FAILURE`
* message explicite (préfixe `GlobalReviewStep:`)

---

## Règles importantes

* ne pas dépendre de `WorkflowStepResult.data` précédent
* reconstruire les chemins via `WorkflowArtifactService`
* pas de parsing complexe
* pas de logique métier globale
* pas d’orchestrator dans la step

---

## Tests

Tests simples :

* cas nominal
* variable manquante → STOP_FAILURE
* fichier d’analyse absent → STOP_FAILURE
* échec Codex → STOP_FAILURE
* artefacts écrits

---

## Contraintes

* ne pas modifier les classes existantes
* ne pas ajouter de nouvelle abstraction
* rester minimal

---

## Traçabilité

* `1.implements.md`
* `result.1.implements.md`
* `1.review.md`
* `result.1.review.md`

---

Implémentation directe attendue, sans sur-conception.
