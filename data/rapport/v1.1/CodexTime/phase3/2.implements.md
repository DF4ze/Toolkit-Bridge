Tu peux passer à l’implémentation.

Ton analyse est validée.

## Objectif

Faire évoluer `WorkflowOrchestrator` pour enchaîner 2 steps de manière minimale.

---

## À faire

Modifier uniquement :

* `WorkflowOrchestrator`
* `WorkflowOrchestratorTest`

---

## Implémentation attendue

Ajouter une méthode :

`executeTwoSteps(WorkflowExecutionContext context, WorkflowStep firstStep, WorkflowStep secondStep)`

---

## Comportement

1. Valider :

   * `context != null`
   * `firstStep != null`
   * `secondStep != null`

2. Exécuter :
   `result1 = executeSingleStep(context, firstStep)`

3. Si `result1.decision != CONTINUE` :
   → retourner `result1`

4. Sinon :
   `result2 = executeSingleStep(context, secondStep)`

5. Retourner `result2`

---

## Règles importantes

* ne pas dupliquer la logique de `executeSingleStep`
* ne pas introduire de liste de steps
* ne pas introduire de moteur
* ne pas introduire de routing
* ne pas introduire de retry
* ne pas modifier le contexte
* ne pas créer de nouvelle classe

---

## Tests

Ajouter dans `WorkflowOrchestratorTest` :

* cas nominal :

  * step1 CONTINUE → step2 exécutée → résultat step2

* cas WAIT_HUMAN sur step1 :

  * step2 non exécutée
  * retour step1

* cas STOP_FAILURE sur step1 :

  * step2 non exécutée
  * retour step1

* cas step2 retourne WAIT_HUMAN :

  * retour step2

---

## Contraintes

* pas de refactor global
* pas de nouvelle abstraction
* rester minimal

---

## Traçabilité

* `2.implements.md`
* `result.2.implements.md`
* `2.review.md`
* `result.2.review.md`

---

Implémentation directe attendue, sans sur-conception.
