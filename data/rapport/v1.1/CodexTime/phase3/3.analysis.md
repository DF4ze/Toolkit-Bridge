Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 3 — Étape 3

Objectif de l’étape :
analyser l’introduction d’un premier vrai arrêt fonctionnel du workflow via `WAIT_HUMAN`, sans encore intégrer Telegram ni un vrai transport humain.

Important :
dans CE prompt, je demande une **analyse uniquement**.

## Règle absolue

### Analyse uniquement — ne pas modifier le code

Tu dois :

* analyser l’existant
* proposer le design
* identifier les classes à créer ou modifier
* signaler les risques
* proposer un plan d’implémentation sobre

Tu ne dois PAS :

* créer de fichier
* modifier du code
* implémenter quoi que ce soit
* lancer une passe de refactor
* prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

---

## 0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase3/`

Nommage attendu pour cette étape :

* prompt d’analyse :
  `3.analysis.md`
* résultat d’analyse :
  `result.3.analysis.md`

Important :

* enregistrer le prompt reçu dans `3.analysis.md`
* enregistrer le rapport d’analyse dans `result.3.analysis.md`

Ne pas produire de fichier `3.implements.md` ni `result.3.implements.md` à ce stade.

---

## 1. Contexte existant

Les briques suivantes existent déjà :

* `WorkflowOrchestrator`

  * exécution d’une step
  * exécution linéaire de 2 steps
* `GlobalAnalysisStep`
* `GlobalReviewStep`
* `WorkflowExecutionContext`
* `WorkflowStep`
* `WorkflowStepResult`
* `WorkflowStepDecision`
* `WorkflowArtifactService`
* `CodexWorkflowClient`

Le système sait déjà :

* enchaîner `GlobalAnalysisStep` puis `GlobalReviewStep`
* s’arrêter sur `STOP_FAILURE`
* continuer sur `CONTINUE`

Nous voulons maintenant introduire un premier arrêt fonctionnel contrôlé :

* `WAIT_HUMAN`

Mais sans encore :

* intégrer Telegram
* ouvrir une boucle homme/machine complète
* créer un moteur de décision avancé

---

## 2. Objectif de l’analyse

Je veux que tu analyses la manière la plus simple et la plus propre d’introduire `WAIT_HUMAN` comme état réel et observable du workflow.

Le but n’est PAS :

* d’implémenter l’échange humain complet
* d’ajouter une messagerie
* de créer un système de tâches humaines
* de faire un moteur de suspension/reprise générique

Je veux seulement préparer un premier arrêt contrôlé, visible, cohérent avec l’existant.

---

## 3. Ce que cette évolution devra permettre plus tard

Au moment de l’implémentation, le système devra pouvoir :

1. permettre à une step de retourner `WAIT_HUMAN`
2. faire en sorte que l’orchestrator s’arrête proprement quand cela arrive
3. retourner un résultat final exploitable
4. rendre cet arrêt observable localement
5. préparer plus tard une future reprise, sans l’implémenter maintenant

Mais dans CE prompt :

* tu ne dois PAS l’implémenter
* tu dois seulement analyser la meilleure manière simple de le faire

---

## 4. Points à analyser explicitement

### A. Où introduire réellement `WAIT_HUMAN`

Je veux que tu indiques :

* si cela doit être porté par une step existante, probablement `GlobalReviewStep`
* ou s’il faut créer une nouvelle step dédiée

Je veux une recommandation nette et pragmatique.

### B. Responsabilité exacte

Définis précisément :

* ce que doit faire la step concernée pour retourner `WAIT_HUMAN`
* ce que doit faire l’orchestrator
* ce qui doit rester hors périmètre

### C. Déclenchement de `WAIT_HUMAN`

Je veux une recommandation simple sur la première règle de déclenchement.

Exemples possibles :

* présence d’un mot-clé explicite dans le résultat de review
* absence d’information suffisante
* convention de test contrôlée
* autre règle très simple

Je veux une solution sobre, testable, non magique.

### D. Rapport à `WorkflowStepResult`

Analyse ce que doit contenir le `WorkflowStepResult` en cas de `WAIT_HUMAN`.

Je veux une recommandation concrète sur :

* `decision`
* `message`
* éventuellement `data`

Mais sans créer un nouveau modèle si ce n’est pas nécessaire.

### E. Rapport à l’orchestrator

Analyse comment `WorkflowOrchestrator` doit réagir à `WAIT_HUMAN` dans le flux 2 steps.

Je veux une réponse explicite :

* arrêt immédiat ?
* propagation du résultat ?
* mise à jour du contexte ?
* autre ?

### F. Rapport au contexte runtime

Analyse si `WorkflowExecutionContext` actuel suffit pour cette étape, ou s’il faut déjà l’enrichir.

Je veux une réponse nette :

1. le contexte actuel suffit
2. le contexte actuel ne suffit pas
3. le contexte actuel suffit avec conventions simples

### G. Observabilité locale

Je veux que tu analyses comment rendre `WAIT_HUMAN` observable localement, sans Telegram.

Par exemple :

* message clair dans `WorkflowStepResult`
* artefact complémentaire
* simple convention dans la review
* autre solution minimale

Je veux une recommandation pragmatique.

### H. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

* Telegram
* système de reprise
* file d’attente humaine
* persistance avancée d’état
* routing conditionnel riche
* moteur de décision
* règles complexes de classification

---

## 5. Question importante

Je veux que tu répondes explicitement à ceci :

### Pour une V1 sobre, vaut-il mieux :

1. faire retourner `WAIT_HUMAN` par `GlobalReviewStep` sur une règle simple,
   ou
2. introduire déjà une step dédiée à la décision humaine ?

Je veux une recommandation nette, argumentée, pragmatique.

Le but est de rester simple et de ne pas créer une abstraction prématurée.

---

## 6. Contraintes fortes

Je veux une solution :

* simple
* lisible
* progressive
* sans framework caché
* sans moteur de reprise
* sans généralisation prématurée
* sans nouvelle couche inutile

Je ne veux pas :

* de Telegram
* de système de task humaine complet
* de queue
* de mécanisme de reprise avancé
* de parser sémantique lourd
* de stratégie configurable complexe

---

## 7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. où introduire `WAIT_HUMAN`
2. les classes à créer ou modifier
3. la responsabilité exacte des composants
4. la règle de déclenchement recommandée
5. le contenu recommandé du `WorkflowStepResult`
6. le rapport au contexte runtime
7. le mode d’observabilité locale recommandé
8. les risques de sur-conception
9. le plan d’implémentation minimal
10. ce qui devra rester volontairement absent

---

## 8. Point de vigilance majeur

Le principal risque de cette étape est de transformer un simple arrêt contrôlé en pseudo système complet de gestion humaine.

Je veux au contraire :

* un premier `WAIT_HUMAN` réel
* simple
* testable
* observable
* sans couche supplémentaire inutile

Tu dois rester strictement dans cette analyse.
