Correction ciblée demandée.

L’implémentation de `WAIT_HUMAN` est validée.
Aucune modification d’architecture n’est demandée.

## Objectif

Améliorer la robustesse de la détection `WAIT_HUMAN`.

---

## 1. Source de vérité de la décision

Actuellement, la décision est basée sur un contenu agrégé (stdout + stderr).

### Correction demandée

* baser la détection uniquement sur **stdout métier**
* ne pas utiliser stderr pour décider

stderr doit rester :

* uniquement informatif
* présent dans les artefacts
* mais sans impact sur la décision

---

## 2. Suppression du round-trip disque

Actuellement :

* écriture `REVIEW_RESULT`
* relecture du fichier
* décision

### Correction demandée

* utiliser directement le contenu en mémoire (`reviewResultContent`)
* ne plus relire le fichier pour décider

L’écriture du fichier reste inchangée (traçabilité).

---

## 3. Tests supplémentaires

Ajouter :

### a. Cas WAIT_HUMAN sans WAIT_REASON

* vérifier absence de `waitReason` dans `data`

### b. Cas faux positif stderr

* `stderr` contient `DECISION: WAIT_HUMAN`
* `stdout` ne contient pas la convention

Attendu :

* decision = `CONTINUE`

---

## Contraintes

* ne pas modifier `WorkflowOrchestrator`
* ne pas ajouter de nouvelle classe
* ne pas introduire de parsing complexe
* ne pas refactorer globalement

---

## Traçabilité

* `3.correction.md`
* `result.3.correction.md`

---

## Livrable

* corrections appliquées
* tests complétés
* build vert
* confirmation

Aucune autre modification.
