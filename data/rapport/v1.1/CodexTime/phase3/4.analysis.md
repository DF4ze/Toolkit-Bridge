Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 3 — Étape 4

Objectif de l’étape :
analyser l’introduction d’une nouvelle step métier :
`CorrectionStep`

Important :
dans CE prompt, je demande une **analyse uniquement**.

## Règle absolue

### Analyse uniquement — ne pas modifier le code

Tu dois :

* analyser l’existant
* proposer le design
* identifier les classes à créer ou modifier
* signaler les risques
* proposer un plan d’implémentation sobre

Tu ne dois PAS :

* créer de fichier
* modifier du code
* implémenter quoi que ce soit
* lancer une passe de refactor
* prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

---

## 0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase3/`

Nommage attendu pour cette étape :

* prompt d’analyse :
  `4.analysis.md`
* résultat d’analyse :
  `result.4.analysis.md`

Important :

* enregistrer le prompt reçu dans `4.analysis.md`
* enregistrer le rapport d’analyse dans `result.4.analysis.md`

Ne pas produire de fichier `4.implements.md` ni `result.4.implements.md` à ce stade.

---

## 1. Contexte existant

Les briques suivantes existent déjà :

* `WorkflowOrchestrator`

  * exécution 1 step
  * exécution linéaire de 2 steps
* `GlobalAnalysisStep`
* `GlobalReviewStep`
* `WorkflowExecutionContext`
* `WorkflowStep`
* `WorkflowStepResult`
* `WorkflowStepDecision`
* `WorkflowArtifactService`
* `WorkflowArtifactType`
* `CodexWorkflowClient`

Le système sait déjà :

* produire des artefacts d’analyse
* produire des artefacts de review
* retourner :

  * `CONTINUE`
  * `STOP_FAILURE`
  * `WAIT_HUMAN`

Nous voulons maintenant introduire une step de correction simple.

---

## 2. Objectif de l’analyse

Je veux que tu analyses comment introduire proprement une step du type :

* `CorrectionStep`

Cette step devra plus tard :

* implémenter `WorkflowStep`
* relire les artefacts précédents utiles
* construire un prompt de correction simple
* appeler `CodexWorkflowClient`
* écrire les artefacts de correction
* retourner un `WorkflowStepResult`

Mais dans CE prompt :

* tu ne dois PAS l’implémenter
* tu dois seulement analyser la meilleure manière simple de le faire

---

## 3. Ce que cette future step devra faire

Au moment de l’implémentation, cette future step devra être capable de :

1. retrouver les artefacts précédents utiles
2. lire au minimum les éléments nécessaires à une correction
3. construire un prompt de correction simple
4. exécuter Codex via le client dédié
5. écrire :

   * `CORRECTION_PROMPT`
   * `CORRECTION_RESULT`
6. retourner un `WorkflowStepResult`

Important :
cette step ne doit pas devenir :

* un orchestrator
* un moteur de retry
* un moteur de décision globale
* un gestionnaire complet de boucle de correction

---

## 4. Points à analyser explicitement

### A. Placement

Propose le package le plus cohérent pour `CorrectionStep`.

### B. Responsabilité exacte

Définis précisément ce que fait `CorrectionStep`, et ce qu’elle ne doit surtout pas faire.

### C. Dépendances minimales

Indique quelles briques existantes elle doit utiliser directement :

* `WorkflowExecutionContext`
* `WorkflowArtifactService`
* `WorkflowArtifactType`
* `CodexWorkflowClient`

Et indique clairement ce qu’elle ne doit pas absorber.

### D. Sources d’entrée de la correction

Je veux que tu répondes explicitement à cette question :

### `CorrectionStep` doit-elle s’appuyer sur :

1. les artefacts d’analyse uniquement,
2. les artefacts de review uniquement,
3. ou un sous-ensemble précis analyse + review ?

Je veux une recommandation nette et argumentée.

### E. Rapport aux artefacts

Analyse comment la step doit retrouver les artefacts nécessaires sans dupliquer la logique de nommage.

### F. Rapport au contexte runtime

Analyse quelles données minimales doivent être disponibles dans `WorkflowExecutionContext` pour que `CorrectionStep` fonctionne proprement.

Je veux une réponse nette :

1. le contexte actuel suffit
2. le contexte actuel ne suffit pas
3. le contexte actuel suffit avec conventions simples

### G. Rapport à Codex

Analyse comment utiliser `CodexWorkflowClient` sans faire dériver la step vers un rôle d’orchestrator.

### H. Résultat de la step

Analyse quel `WorkflowStepResult` cette step doit raisonnablement retourner dans une première version.

Je veux une recommandation simple et concrète.

### I. Rapport à `WAIT_HUMAN`

Je veux que tu répondes explicitement à ceci :

### `CorrectionStep` doit-elle être exécutée uniquement après un résultat de review particulier, ou doit-elle rester indépendante et simplement supposer que les artefacts nécessaires existent ?

Je veux une recommandation nette.

### J. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

* boucle automatique de retry
* compteur de tentatives
* orchestration complète de correction
* Telegram
* Maven
* décisions humaines avancées
* parsing sémantique lourd
* moteur de planification

---

## 5. Question importante

Je veux que tu répondes explicitement à ceci :

### Pour une V1 sobre, vaut-il mieux :

1. faire une `CorrectionStep` réutilisable dès qu’on a les artefacts nécessaires,
   ou
2. la coupler explicitement au cas `WAIT_HUMAN` / review négative dès maintenant ?

Je veux une recommandation nette, pragmatique, sans préparer un framework.

---

## 6. Contraintes fortes

Je veux une solution :

* simple
* lisible
* progressive
* sans framework caché
* sans moteur de boucle
* sans généralisation prématurée
* sans nouvelle couche inutile

Je ne veux pas :

* de retry automatique
* de file de corrections
* de moteur de priorisation
* de Telegram
* de validation Maven
* de stratégie configurable complexe

---

## 7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. le package recommandé
2. les classes à créer ou modifier
3. la responsabilité exacte de `CorrectionStep`
4. les dépendances minimales
5. les artefacts d’entrée recommandés
6. le besoin éventuel d’ajustement du contexte runtime
7. le choix recommandé sur le lien avec `WAIT_HUMAN`
8. les risques de sur-conception
9. le plan d’implémentation minimal
10. ce qui devra rester volontairement absent

---

## 8. Point de vigilance majeur

Le principal risque de cette étape est de transformer `CorrectionStep` en :

* moteur de retry
* mini orchestrator
* ou gestionnaire générique de “réparation” du workflow

Je veux au contraire :

* une step métier unique
* claire
* bornée
* qui produit une correction simple à partir des artefacts existants

Tu dois rester strictement dans cette analyse.
