Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 4 — Étape 1

Objectif de l’étape :
analyser l’évolution minimale de l’orchestrator pour permettre un enchaînement explicite à 3 steps :

* `GlobalAnalysisStep`
* `GlobalReviewStep`
* `CorrectionStep`

Important :
dans CE prompt, je demande une **analyse uniquement**.

## Règle absolue

### Analyse uniquement — ne pas modifier le code

Tu dois :

* analyser l’existant
* proposer le design
* identifier les classes à créer ou modifier
* signaler les risques
* proposer un plan d’implémentation sobre

Tu ne dois PAS :

* créer de fichier
* modifier du code
* implémenter quoi que ce soit
* lancer une passe de refactor
* prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

---

## 0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase4/`

Nommage attendu pour cette étape :

* prompt d’analyse :
  `1.analysis.md`
* résultat d’analyse :
  `result.1.analysis.md`

Important :

* enregistrer le prompt reçu dans `1.analysis.md`
* enregistrer le rapport d’analyse dans `result.1.analysis.md`

Ne pas produire de fichier `1.implements.md` ni `result.1.implements.md` à ce stade.

---

## 1. Contexte existant

Les briques suivantes existent déjà :

* `WorkflowOrchestrator`

  * exécution 1 step
  * exécution 2 steps
* `GlobalAnalysisStep`
* `GlobalReviewStep`
* `CorrectionStep`
* `WorkflowExecutionContext`
* `WorkflowStep`
* `WorkflowStepResult`
* `WorkflowStepDecision`
* `WorkflowArtifactService`
* `WorkflowArtifactType`
* `CodexWorkflowClient`

Le système sait déjà :

* exécuter 1 step
* exécuter 2 steps linéaires
* produire `WAIT_HUMAN`
* produire `STOP_FAILURE`
* produire des artefacts d’analyse, review et correction

Nous voulons maintenant permettre un enchaînement explicite à 3 steps.

---

## 2. Objectif de l’analyse

Je veux que tu analyses comment faire évoluer proprement l’orchestrator pour supporter un flux strictement linéaire de 3 steps :

1. `GlobalAnalysisStep`
2. `GlobalReviewStep`
3. `CorrectionStep`

Le but n’est PAS d’introduire :

* un moteur générique
* une liste dynamique de steps
* une state machine
* un système de routing
* un pipeline abstrait

Je veux une évolution minimale, concrète, lisible.

---

## 3. Ce que cette évolution devra permettre plus tard

Au moment de l’implémentation, le système devra pouvoir :

1. exécuter `GlobalAnalysisStep`
2. lire le résultat
3. si `CONTINUE`, exécuter `GlobalReviewStep`
4. lire le résultat
5. si `CONTINUE`, exécuter `CorrectionStep`
6. si une step retourne `WAIT_HUMAN` ou `STOP_FAILURE`, s’arrêter immédiatement
7. retourner un résultat final simple et cohérent

Mais dans CE prompt :

* tu ne dois PAS l’implémenter
* tu dois seulement analyser la meilleure manière simple de le faire

---

## 4. Points à analyser explicitement

### A. Package / emplacement

Indique si l’évolution doit rester dans `WorkflowOrchestrator`, ou s’il faut introduire une autre petite classe.
Je veux une recommandation sobre.

### B. Responsabilité exacte

Définis précisément ce que doit faire cette version “3 steps” de l’orchestrator, et ce qu’elle ne doit surtout pas faire.

### C. Signature recommandée

Propose la signature la plus simple pour exécuter trois steps.
Je veux une recommandation nette.

### D. Gestion des décisions

Analyse comment gérer strictement :

* `CONTINUE`
* `WAIT_HUMAN`
* `STOP_FAILURE`

Je veux une recommandation explicite sur :

* quand lancer la step suivante
* quand s’arrêter
* quelle décision finale retourner

### E. Rapport au contexte runtime

Analyse si le même `WorkflowExecutionContext` peut être transmis tel quel aux 3 steps, ou s’il faut déjà le faire évoluer.

Je veux une réponse nette :

1. le contexte actuel suffit tel quel
2. il faut le modifier maintenant
3. il suffit tel quel pour cette étape, avec limites assumées

### F. Rapport aux résultats intermédiaires

Je veux que tu répondes explicitement à ceci :

### Les steps suivantes doivent-elles dépendre des `WorkflowStepResult` précédents, ou seulement s’appuyer sur les artefacts déjà écrits ?

Je veux une recommandation nette et argumentée.

### G. Résultat final de l’orchestrator

Analyse quel objet doit être retourné par cette exécution à 3 steps :

* le résultat de la dernière step ?
* le premier résultat bloquant ?
* autre chose ?

Je veux une solution simple, sans nouveau modèle si possible.

### H. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

* nombre variable de steps
* retry
* correction automatique conditionnelle
* décisions humaines avancées
* Telegram
* Maven
* moteur de transitions
* multi-lots

---

## 5. Question importante

Je veux que tu répondes explicitement à ceci :

### Faut-il faire évoluer `WorkflowOrchestrator` avec une simple nouvelle méthode dédiée à l’enchaînement de 3 steps, ou créer déjà une abstraction du type “workflow sequence” ?

Je veux une recommandation nette.

Le but est de rester pragmatique, pas de préparer un framework.

---

## 6. Contraintes fortes

Je veux une solution :

* simple
* lisible
* progressive
* sans framework caché
* sans généralisation prématurée
* sans state machine
* sans abstraction artificielle

Je ne veux pas :

* de liste dynamique de steps
* de routing
* de plugin system
* de stratégie configurable
* de modèle complexe de transition

---

## 7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. la recommandation d’évolution de l’orchestrator
2. les classes à créer ou modifier
3. la signature recommandée
4. la gestion des décisions
5. le rapport au contexte runtime
6. le rapport aux résultats intermédiaires
7. les risques de sur-conception
8. le plan d’implémentation minimal
9. ce qui devra rester volontairement absent

---

## 8. Point de vigilance majeur

Le principal risque de cette étape est de transformer un simple enchaînement de 3 steps en pseudo moteur de workflow générique.

Je veux au contraire :

* une évolution minimale
* claire
* bornée
* qui enchaîne seulement 3 steps
* et rien de plus

Tu dois rester strictement dans cette analyse.
