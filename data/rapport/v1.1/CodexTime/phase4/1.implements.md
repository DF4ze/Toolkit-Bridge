Tu peux passer à l’implémentation.

Ton analyse est validée.

## Objectif

Ajouter un enchaînement explicite à 3 steps dans `WorkflowOrchestrator`.

---

## À modifier

* `WorkflowOrchestrator`
* `WorkflowOrchestratorTest`

---

## Implémentation attendue

Ajouter :

`executeThreeSteps(WorkflowExecutionContext context, WorkflowStep firstStep, WorkflowStep secondStep, WorkflowStep thirdStep)`

---

## Comportement

1. Valider :

   * `context != null`
   * `firstStep != null`
   * `secondStep != null`
   * `thirdStep != null`

2. Exécuter :
   `result1 = executeSingleStep(context, firstStep)`

3. Si `result1.decision != CONTINUE` :
   → retourner `result1`

4. Exécuter :
   `result2 = executeSingleStep(context, secondStep)`

5. Si `result2.decision != CONTINUE` :
   → retourner `result2`

6. Exécuter :
   `result3 = executeSingleStep(context, thirdStep)`

7. Retourner `result3`

---

## Règles importantes

* ne pas dupliquer la logique de `executeSingleStep`
* ne pas introduire de liste de steps
* ne pas introduire de moteur
* ne pas introduire de routing
* ne pas modifier le contexte
* ne pas créer de nouvelle classe

---

## Tests

Ajouter :

### 1. Cas nominal

* step1 CONTINUE
* step2 CONTINUE
* step3 exécutée
  → résultat = step3

### 2. Blocage step1

* step1 WAIT_HUMAN ou STOP_FAILURE
  → step2 et step3 non exécutées

### 3. Blocage step2

* step1 CONTINUE
* step2 WAIT_HUMAN ou STOP_FAILURE
  → step3 non exécutée

### 4. step3 WAIT_HUMAN

→ résultat = step3

---

## Contraintes

* pas de refactor global
* pas de nouvelle abstraction
* rester minimal

---

## Traçabilité

* `1.implements.md`
* `result.1.implements.md`
* `1.review.md`
* `result.1.review.md`

---

Implémentation directe attendue, sans sur-conception.
