Tu travailles sur le projet Toolkit-Bridge.

Le workflow de référence est :
file:/D:/Documents/Spring/Toolkit-Bridge/data/rapport/00.promptWorkflow/0.workflow.md

Nous travaillons maintenant sur :
Phase 4 — Étape 2

Objectif de l’étape :
analyser comment conditionner l’exécution de `CorrectionStep` au lieu de l’exécuter systématiquement.

Concrètement, le workflow visé devient :

- `GlobalAnalysisStep`
- `GlobalReviewStep`
- `CorrectionStep` seulement si la review l’exige

Important :
dans CE prompt, je demande une analyse uniquement.

Règle absolue

Analyse uniquement — ne pas modifier le code

Tu dois :

- analyser l’existant
- proposer le design
- identifier les classes à créer ou modifier
- signaler les risques
- proposer un plan d’implémentation sobre

Tu ne dois PAS :

- créer de fichier source
- modifier du code
- implémenter quoi que ce soit
- lancer une passe de refactor
- prendre de l’avance sur l’implémentation

Toute modification de code dans cette phase serait considérée comme une erreur de workflow.

0. Règles de traçabilité documentaire

Les artefacts doivent être enregistrés ici :

`data/rapport/v1.1/CodexTime/Phase4/`

Nommage attendu pour cette étape :

- prompt d’analyse : `2.analysis.md`
- résultat d’analyse : `result.2.analysis.md`

Important :

- enregistrer le prompt reçu dans `2.analysis.md`
- enregistrer le rapport d’analyse dans `result.2.analysis.md`

Ne pas produire de fichier `2.implements.md` ni `result.2.implements.md` à ce stade.

1. Contexte existant

Les briques suivantes existent déjà :

- `WorkflowOrchestrator`
  - exécution 1 step
  - exécution 2 steps
  - exécution 3 steps linéaires
- `GlobalAnalysisStep`
- `GlobalReviewStep`
- `CorrectionStep`
- `WorkflowExecutionContext`
- `WorkflowStep`
- `WorkflowStepResult`
- `WorkflowStepDecision`
- `WorkflowArtifactService`
- `WorkflowArtifactType`
- `CodexWorkflowClient`

Le système sait déjà :

- enchaîner `Analysis -> Review -> Correction`
- s’arrêter sur `STOP_FAILURE`
- s’arrêter sur `WAIT_HUMAN`

Nous voulons maintenant rendre l’exécution de `CorrectionStep` conditionnelle.

2. Objectif de l’analyse

Je veux que tu analyses comment faire évoluer proprement l’orchestrator pour permettre la règle suivante :

- exécuter `GlobalAnalysisStep`
- exécuter `GlobalReviewStep`
- exécuter `CorrectionStep` uniquement si la review demande une correction
- sinon arrêter proprement après la review

Le but n’est PAS d’introduire :

- un moteur de règles
- un parser sémantique complexe
- une stratégie configurable
- un moteur générique de workflow

Je veux une évolution minimale, concrète, lisible.

3. Ce que cette évolution devra permettre plus tard

Au moment de l’implémentation, le système devra pouvoir :

1. exécuter `GlobalAnalysisStep`
2. si `CONTINUE`, exécuter `GlobalReviewStep`
3. si `GlobalReviewStep` retourne `WAIT_HUMAN` ou `STOP_FAILURE`, s’arrêter immédiatement
4. si `GlobalReviewStep` retourne `CONTINUE`, décider :
   - lancer `CorrectionStep`
   - ou s’arrêter proprement sans correction
5. retourner un résultat final simple et cohérent

Mais dans CE prompt :

- tu ne dois PAS l’implémenter
- tu dois seulement analyser la meilleure manière simple de le faire

4. Points à analyser explicitement

A. Où placer la logique de condition

Je veux une réponse nette :

- dans `WorkflowOrchestrator`
- dans `GlobalReviewStep`
- ailleurs

Je veux une recommandation pragmatique.

B. Source de vérité de la décision

Je veux que tu répondes explicitement à ceci :

La décision “faut-il lancer `CorrectionStep` ?” doit-elle se baser :

1. sur `WorkflowStepResult` de `GlobalReviewStep`
2. sur l’artefact `REVIEW_RESULT`
3. sur une combinaison simple des deux

Je veux une recommandation nette et argumentée.

C. Convention recommandée

Je veux une règle V1 simple et testable.

Exemples possibles :

- `DECISION: NEED_CORRECTION`
- `DECISION: OK`
- autre convention minimale

Je veux une recommandation explicite, sobre, non magique.

D. Signature de l’orchestrator

Analyse comment faire évoluer l’orchestrator sans sur-conception.

Je veux une réponse nette :

- simple nouvelle méthode dédiée ?
- adaptation de `executeThreeSteps(...)` ?
- autre ?

E. Gestion des décisions

Analyse comment gérer strictement :

- `CONTINUE`
- `WAIT_HUMAN`
- `STOP_FAILURE`

Et en plus :

- le cas “review continue mais sans correction requise”

Je veux une recommandation claire sur le résultat final à retourner dans chacun des cas.

F. Rapport au contexte runtime

Analyse si le contexte actuel suffit pour cette étape.

Je veux une réponse nette :

1. le contexte actuel suffit
2. il faut le modifier maintenant
3. il suffit tel quel avec conventions simples

G. Rapport à `CorrectionStep`

Je veux que tu répondes explicitement à ceci :

`CorrectionStep` doit-elle rester totalement indépendante et simplement être appelée ou non par l’orchestrator, ou faut-il commencer à l’enrichir pour connaître la raison de son appel ?

Je veux une recommandation nette.

H. Observabilité locale

Je veux une recommandation pragmatique sur comment rendre visible le fait que :

- la review est OK et aucune correction n’est nécessaire
- ou au contraire qu’une correction a été déclenchée

Sans Telegram, sans nouvelle couche technique.

I. Limites de périmètre

Signale explicitement ce qui doit rester hors de cette étape :

- parsing sémantique lourd
- moteur de règles
- stratégie configurable
- retry automatique
- boucle de correction
- Telegram
- Maven
- multi-lots

5. Question importante

Je veux que tu répondes explicitement à ceci :

Pour une V1 sobre, vaut-il mieux :

1. faire porter à `GlobalReviewStep` une convention explicite du type `DECISION: NEED_CORRECTION`,
2. ou essayer de déduire implicitement le besoin de correction depuis le texte libre de la review ?

Je veux une recommandation nette, argumentée, pragmatique.

Le but est de rester simple et testable.

6. Contraintes fortes

Je veux une solution :

- simple
- lisible
- progressive
- sans framework caché
- sans moteur de règles
- sans généralisation prématurée
- sans nouvelle couche inutile

Je ne veux pas :

- de parsing sémantique avancé
- de stratégie configurable
- de moteur de planification
- de système de retry
- de couplage Telegram
- de validation Maven

7. Attendu du rapport d’analyse

Je veux un rapport clair qui précise :

1. où placer la logique de condition
2. la source de vérité recommandée
3. la convention recommandée
4. les classes à créer ou modifier
5. la signature recommandée
6. le rapport au contexte runtime
7. le rapport à `CorrectionStep`
8. le mode d’observabilité locale recommandé
9. les risques de sur-conception
10. le plan d’implémentation minimal
11. ce qui devra rester volontairement absent

8. Point de vigilance majeur

Le principal risque de cette étape est de transformer une simple condition d’exécution en pseudo moteur de règles.

Je veux au contraire :

- une convention explicite
- une décision simple
- un orchestrator lisible
- aucun framework caché

Tu dois rester strictement dans cette analyse.
