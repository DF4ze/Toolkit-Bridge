Correction de robustesse demandée.

L’implémentation est validée.
Aucune modification d’architecture n’est attendue.

Objectif

Rendre le comportement déterministe et sécuriser le contrat entre `GlobalReviewStep` et `WorkflowOrchestrator`.

1. Contrat strict `requiresCorrection`

Problème actuel

Si la clé `requiresCorrection` est absente ou invalide :
-> fallback silencieux à `false`

Correction demandée

Dans `WorkflowOrchestrator` :

Si :
- décision = `CONTINUE`
- et `requiresCorrection` absent ou invalide

Alors :
-> lever une erreur explicite (`IllegalStateException`)
ou retourner `STOP_FAILURE` avec message clair

2. Priorité des directives review

Problème actuel

Plusieurs directives possibles -> ordre dépendant du texte

Correction demandée

Dans `GlobalReviewStep` :

Implémenter une règle explicite :

```text
WAIT_HUMAN > NEED_CORRECTION > OK
```

Comportement

- scanner tout le contenu
- détecter tous les marqueurs
- appliquer la priorité une seule fois

3. Tests à ajouter

a. Contrat invalide

- review CONTINUE
- `requiresCorrection` absent

-> attendu : erreur explicite

b. Priorité directives

Cas :

```text
DECISION: OK
DECISION: NEED_CORRECTION
```

-> attendu : NEED_CORRECTION

Cas :

```text
DECISION: NEED_CORRECTION
DECISION: WAIT_HUMAN
```

-> attendu : WAIT_HUMAN

Contraintes

- ne pas modifier `CorrectionStep`
- ne pas modifier `WorkflowExecutionContext`
- ne pas introduire de nouvelle classe
- ne pas ajouter de moteur
- rester minimal

Traçabilité

- `2.correction.md`
- `result.2.correction.md`

Livrable

- corrections appliquées
- tests complétés
- build vert
- confirmation

Aucune autre modification.
