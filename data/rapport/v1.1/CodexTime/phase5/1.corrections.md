Correction structurelle légère demandée.

L’implémentation est validée.
Objectif : isoler le wiring runtime pour éviter la duplication future.

---

## Objectif

Extraire l’assemblage du runner hors du CLI, sans introduire de nouvelle couche métier.

---

## 1. Créer une factory minimale

Créer une classe :

AnalysisReviewWorkflowRunnerFactory

Responsabilité :

* construire une instance complète de `AnalysisReviewWorkflowRunner`
* encapsuler le wiring actuel de `defaultRunner()`

---

## 2. Déplacer le wiring

Depuis `AnalysisReviewWorkflowCli.defaultRunner()` :

* déplacer toute la logique d’assemblage dans la factory
* garder le CLI comme simple consommateur

---

## 3. Adapter le CLI

Remplacer :

defaultRunner()

par :

AnalysisReviewWorkflowRunnerFactory.createDefault()

---

## 4. Contraintes

* aucune logique métier
* aucun changement de comportement
* aucune nouvelle abstraction
* pas de Spring
* pas de refactor global

---

## 5. Bonus léger (optionnel)

Dans le CLI :

* ajouter constantes privées pour :

  * clés variables (`reportRootDirectory`, etc.)
  * clés de sortie (`finalDecision`, `nextAction`, etc.)

---

## 6. Tests

* vérifier que le CLI fonctionne toujours
* pas besoin de nouveaux tests complexes

---

## 7. Traçabilité

* `1.correction.md`
* `result.1.correction.md`

---

Objectif : préparer les futurs points d’entrée sans complexifier maintenant.
