# Relecture - Phase 5 Etape 2

Relire l'implementation de stabilisation du contrat externe.

Verifier:
- absence de modification de `WorkflowStepResult`;
- absence de DTO ou projection complexe;
- presence des cles obligatoires `finalDecision`, `nextAction`, `correctionTriggered`;
- exposition CLI de `correctionTriggered`, `waitReason` et `workflowSummaryPath` conditionnel;
- normalisation `key=value` sans valeur null et sans retour ligne;
- tests utiles pour RUN, RESUME, WAIT_HUMAN, STOP_FAILURE et absence de summary.

Ne pas ajouter de nouvelle fonctionnalite.
