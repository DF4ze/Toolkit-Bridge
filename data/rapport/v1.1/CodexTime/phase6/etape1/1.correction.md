# PROMPT — Correction Phase 6 Étape 1 — Validation locale minimale

## Contexte

Suite à la revue critique de l'étape 1, quelques corrections mineures sont nécessaires.

Objectif :

* améliorer la robustesse
* clarifier les intentions
* sécuriser le comportement

Sans modifier l'architecture globale.

---

## Corrections à appliquer

### 1. Restaurer le flag d'interruption dans `safeGet()`

Dans `WorkflowValidationService` :

* si `InterruptedException` est catchée :

  * appeler `Thread.currentThread().interrupt()`

Comportement attendu :

* ne pas perdre l'information d'interruption
* rester cohérent avec les bonnes pratiques Java

---

### 2. Ajouter assertions sur `errorMessage`

Dans les tests :

* pour SUCCESS :

  * vérifier que `errorMessage == null`
* pour FAILURE :

  * vérifier que `errorMessage == null`

Objectif :

* valider explicitement le contrat du modèle

---

### 3. Nettoyer double vérification dans `ValidationCommand`

* éviter double check sur index 0
* boucle commence à `i = 1`

---

### 4. Ajouter documentation sur `ValidationResult.errorMessage`

Ajouter un commentaire ou Javadoc :

* `null` si SUCCESS ou FAILURE
* non null uniquement pour SYSTEM_ERROR ou TIMEOUT

---

### 5. Clarifier statut Spring du service

Dans `WorkflowValidationService` :

Ajouter une Javadoc de classe indiquant :

* que la classe n'est pas encore un `@Service`
* que l'annotation sera ajoutée lors du branchement runtime

---

### 6. Extraire constante PowerShell dans test

Dans `WorkflowValidationServiceTest` :

* remplacer le chemin en dur par une constante :

`WINDOWS_POWERSHELL_PATH`

---

## Contraintes strictes

* Ne pas modifier le comportement fonctionnel
* Ne pas modifier l'API publique
* Ne pas brancher dans le runner
* Ne pas introduire Spring
* Ne pas ajouter de nouvelles features

---

## Résultat attendu

Créer :

`data/rapport/v1.1/CodexTime/Phase6/etape1/result_1_correction.md`

Contenu :

* corrections appliquées
* fichiers modifiés
* validation des tests
* confirmation qu'aucune régression n'a été introduite
