# PROMPT — Implémentation Phase 6 Étape 1 — Validation locale minimale

## Contexte

Tu interviens sur `Toolkit-Bridge`.

Nous sommes en :

`v1.1 / CodexTime / Phase6 / etape1`

Objectif : introduire une brique minimale de validation locale, sans encore la brancher dans le workflow runtime.

Le rapport d'analyse a conclu que l'implémentation est prête et qu'il faut s'inspirer du pattern existant de `CodexWorkflowClient`, sans le réutiliser directement.

## Objectif

Créer une brique dédiée permettant d'exécuter une commande locale contrôlée et de récupérer un résultat structuré.

## Fichiers à créer

Package principal :
`src/main/java/fr/ses10doigts/toolkitbridge/service/agent/workflow/runtime/validation/`

* `ValidationStatus.java`
* `ValidationCommand.java`
* `ValidationResult.java`
* `WorkflowValidationService.java`

Package test :
`src/test/java/fr/ses10doigts/toolkitbridge/service/agent/workflow/runtime/validation/`

* `WorkflowValidationServiceTest.java`
* `ValidationCommandTest.java`

## Contraintes strictes

* Ne modifie pas `AnalysisReviewWorkflowRunner`
* Ne modifie pas `WorkflowStepResult`
* Ne branche pas encore la validation dans les steps
* Ne lance pas Maven automatiquement
* Ne crée pas de `ValidationStep`
* Ne passe jamais par `sh -c` ou `cmd /c` dans le service lui-même

## Rapport attendu

`data/rapport/v1.1/CodexTime/Phase6/etape1/result_1_implements.md`
