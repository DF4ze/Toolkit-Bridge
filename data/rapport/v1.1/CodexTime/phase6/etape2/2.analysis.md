# PROMPT — Analyse Phase 6 Étape 2 — Ajouter un build Maven

## Contexte

Tu interviens sur `Toolkit-Bridge`.

La brique `WorkflowValidationService` est maintenant implémentée et testée.

Elle permet d'exécuter une commande locale et de récupérer un résultat structuré.

---

## Objectif

Analyser comment intégrer une première validation Maven :

```
mvn clean compile -DskipTests
```

Sans casser l'architecture existante.

---

## Travail demandé

Analyse le dépôt et réponds précisément :

### 1. Où intégrer la validation Maven ?

* dans un step dédié ?
* dans `GlobalReviewStep` ?
* dans `CorrectionStep` ?
* ailleurs ?

👉 Justifier clairement.

---

### 2. Faut-il créer un `ValidationStep` ?

* oui / non
* pourquoi
* avantages / inconvénients

---

### 3. Quelle commande exacte utiliser ?

* `mvn` vs `mvnw`
* Windows vs Linux
* workingDirectory à utiliser

---

### 4. Comment gérer le résultat ?

À partir de `ValidationResult` :

* mapping vers `WorkflowStepDecision` :

  * SUCCESS → ?
  * FAILURE → ?
  * TIMEOUT → ?
  * SYSTEM_ERROR → ?

---

### 5. Comment intégrer dans le cycle actuel ?

* après `implements`
* après `correction`
* ailleurs ?

---

### 6. Quels impacts sur :

* `WorkflowStepResult`
* `AnalysisReviewWorkflowRunner`
* les steps existants

---

### 7. Quels risques ?

* lenteur
* faux positifs
* dépendance environnement
* multi-OS

---

### 8. Plan d'implémentation

Proposer :

👉 5 à 10 étapes max

---

## Contraintes strictes

* ne pas implémenter
* ne pas modifier le runner
* ne pas introduire Telegram
* ne pas lancer encore Maven
* ne pas sur-architecturer

---

## Résultat attendu

Créer :

`data/rapport/v1.1/CodexTime/Phase6/etape2/result_2_analysis.md`

Contenu :

* résumé exécutif
* stratégie d'intégration
* choix techniques
* risques
* plan clair
* conclusion : prêt / partiellement prêt / pas prêt
