# PROMPT — Correction Phase 6 Étape 5 — Stabilisation summary

Contexte:
- Le summary a été standardisé.
- La revue a signalé trois points à sécuriser: source du status, clés d’artefacts, tests trop fragiles.

Corrections à appliquer:
1. Source du `Status`:
   - utiliser `decision().name()`
   - fallback possible sur `data.finalDecision`
2. Centraliser les clés d’artefacts dans `AnalysisReviewWorkflowRunner`:
   - `REVIEW_RESULT_PATH`
   - `BUILD_RESULT_PATH`
   - `CORRECTION_ATTEMPT_PATH`
3. Assouplir les tests summary:
   - garder les assertions sur `Status`, `Reason`, `Actions`
   - réduire la dépendance aux phrases exactes

Contraintes:
- ne pas modifier la structure du summary
- ne pas changer le contrat CLI
- ne pas ajouter de nouvelles fonctionnalités
- ne pas faire de refactor global
