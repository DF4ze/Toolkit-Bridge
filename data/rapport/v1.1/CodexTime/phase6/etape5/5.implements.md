# PROMPT — Implémentation Phase 6 Étape 5 — Summary exploitable

Contexte:
- Le summary actuel fonctionne mais reste trop technique.
- Objectif: le transformer en interface humaine standardisée.

Travail demandé:
1. Modifier `buildSummaryContent(...)` dans `AnalysisReviewWorkflowRunner`.
2. Appliquer la structure:
   - `Status: ...`
   - `Reason: ...`
   - `Context: ...`
   - `Actions:`
   - `Artifacts:`
3. Adapter le contenu selon le statut:
   - `SUCCESS`
   - `STOP_FAILURE`
   - `WAIT_HUMAN` review
   - `WAIT_HUMAN` build
4. Simplifier les messages.
5. Nettoyer les artifacts.
6. Conserver la compatibilité:
   - ne pas casser `workflowSummaryPath`
   - ne pas modifier le CLI
   - ne pas changer `WorkflowStepResult`
7. Ajouter des tests pour:
   - format SUCCESS
   - format STOP_FAILURE
   - format WAIT_HUMAN build
   - format WAIT_HUMAN review

Contraintes:
- pas de moteur template
- pas de nouvelle couche
- pas de Telegram
- pas de parsing complexe
