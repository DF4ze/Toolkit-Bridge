Nous faisons un dernier tour de correction sur la Phase 1 avant clôture.

Contexte :
La phase 1 est globalement réussie :
- correction `ERROR` propre et robuste
- pas de dérive de périmètre
- couverture utile ajoutée sur `listAgents`, `listRecentArtifacts` et `getOverview`

La relecture a cependant identifié quelques améliorations de qualité test à apporter avant de considérer la phase totalement terminée.

Objectif de cette correction :
- améliorer la robustesse et la maintenabilité des tests ajoutés
- réduire le couplage des tests à l’implémentation interne
- corriger un risque de faux positif Mockito
- nettoyer légèrement le fichier de test
- ne pas changer le périmètre fonctionnel
- ne pas toucher à l’architecture
- ne pas commencer la phase suivante

Points à corriger :

## 1. Réduire les vérifications trop internes
Dans `TechnicalAdminFacadeTest`, remplacer autant que possible les `verify(...)` les plus internes par :
- des assertions sur le contrat observable DTO
- des assertions sur le résultat final
- tout en conservant les `verify(...)` réellement utiles sur les embranchements critiques, notamment :
    - priorité des filtres artefacts
    - fallback policy quand runtime absent

L’idée est :
- garder les `verify` seulement là où ils servent à figer une vraie règle de branchement
- éviter de figer inutilement l’implémentation interne

## 2. Corriger le risque de faux positif Mockito
Le point signalé est important :
- `never().getRequired(anyString())` ne couvre pas un appel avec `null`

Corriger ce cas avec une vérification robuste couvrant aussi `null`, par exemple via `any()` ou autre approche équivalente saine.

## 3. Améliorer légèrement la lisibilité du test
Le fichier `TechnicalAdminFacadeTest` est devenu dense.
Sans créer de nouvelle couche ni refacto excessif :
- factoriser un peu le setup répétitif
- introduire quelques fixtures/helpers privés lisibles si cela simplifie vraiment le fichier
- rester simple
- ne pas sur-ingénierer

## 4. Nettoyage mineur
- supprimer l’import inutilisé `isNull` s’il est toujours présent
- nettoyer les éventuels petits résidus inutiles introduits pendant la phase

Contraintes importantes :
- ne pas modifier `TechnicalAdminFacade` sauf nécessité absolue liée aux tests
- ne pas changer le comportement fonctionnel
- ne pas modifier les DTO
- ne pas extraire de service
- ne pas toucher aux contrôleurs
- rester strictement dans le périmètre de finition de la phase 1

Validation attendue :
Relancer les tests ciblés pertinents :
- `TechnicalAdminFacadeTest`
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

Livrables attendus :
- code ajusté
- tests nettoyés/corrigés
- résumé clair
- liste des fichiers modifiés

Important :
Créer aussi un mini-rapport Markdown dans :
`data/rapport/v1.1/lot1/result_1.1_phase-1-corrections.md`

Le rapport doit contenir exactement :

# Phase 1 — Corrections finales

## 1. Corrections apportées sur les tests
## 2. Réduction du couplage à l’implémentation
## 3. Correction Mockito
## 4. Nettoyage effectué
## 5. Résultats des tests
## 6. Fichiers modifiés

Et dans la sortie terminal, afficher aussi un résumé court avec :
- les principaux tests simplifiés
- la correction Mockito réalisée
- le statut final des tests