Nous passons maintenant à l’implémentation de la Phase 1.

Contexte :
L’analyse de phase a confirmé que cette phase doit rester une phase de sécurisation comportementale avant extraction.
Aucun changement d’architecture large n’est attendu ici.
L’objectif est de verrouiller le comportement actuel de `TechnicalAdminFacade` sur les zones sensibles, puis de corriger localement le point fragile autour de `ERROR`.

Objectif de cette phase :
- renforcer la couverture unitaire de `TechnicalAdminFacade`
- verrouiller les comportements de `listAgents()`
- verrouiller les comportements de `listRecentArtifacts()`
- verrouiller les comportements de `getOverview()`
- corriger la logique fragile basée sur `"ERROR"`
- ne pas encore extraire de services
- ne pas modifier les contrôleurs ni les contrats DTO

Travail demandé :
1. Relire rapidement l’analyse de phase et vérifier si un point doit être corrigé ou ajusté avant implémentation.
2. Si un micro-ajustement d’analyse est nécessaire, applique-le de manière minimale et justifiée.
3. Implémente ensuite entièrement la phase 1.

Implémentation attendue :

## A. Tests à ajouter / compléter dans `TechnicalAdminFacadeTest`
Ajouter des tests unitaires ciblés sur la façade pour couvrir au minimum :

### `listAgents()`
- cas runtime présent
    - la policy/runtime/exposedTools proviennent du runtime
    - pas de fallback inutile via policy registry
- cas runtime absent
    - fallback via `policyRegistry.getRequired(...)`
    - résolution correcte des outils exposés
- cas de normalisation / tri des tools exposés en fallback
- stabilité du mapping principal de `AgentItem`

### `listRecentArtifacts()`
- contrat de priorité des filtres :
    - `taskId` prioritaire sur `agentId`
    - `agentId` utilisé si `taskId` absent ou blank
    - fallback global sinon
- tri descendant par `createdAt`
- application correcte de la limite effective

### `getOverview()`
- agrégation correcte des sous-listes
- calcul correct de `busyAgents`
- calcul correct de `recentErrors`
- cohérence entre tailles/listes/compteurs
- propagation correcte de la limite après sanitation

## B. Correction ciblée du point fragile `ERROR`
Dans `TechnicalAdminFacade#getOverview` :
- remplacer la logique basée sur la string `"ERROR"`
- utiliser la comparaison enum robuste avec `AgentTraceEventType.ERROR`

Cette correction doit être :
- minimale
- locale
- sans changement de contrat externe

## C. Validation de non-régression
Exécuter les tests ciblés pertinents :
- `TechnicalAdminFacadeTest`
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

Contraintes importantes :
- ne pas commencer la phase d’extraction
- ne pas introduire de nouveaux services
- ne pas toucher aux contrôleurs sauf nécessité technique absolue
- ne pas modifier les DTO `TechnicalAdminView`
- ne pas étendre le périmètre à une refonte de l’admin technique
- privilégier des tests orientés comportement observable
- éviter des tests trop couplés à l’implémentation interne

Résultat attendu :
- code modifié
- tests ajoutés ou ajustés
- exécution des tests ciblés
- résumé clair des modifications
- points d’attention éventuels
- liste des fichiers modifiés

Important :
En sortie, je veux aussi un mini-rapport Markdown enregistré dans :
`data/rapport/v1.1/lot2/1.1_result_phase-1-implementation.md`

Le rapport doit contenir exactement :

# Phase 1 — Implémentation

## 1. Ajustements éventuels de l’analyse
## 2. Modifications réalisées
## 3. Tests ajoutés ou modifiés
## 4. Correction apportée sur `ERROR`
## 5. Résultats d’exécution des tests
## 6. Points d’attention
## 7. Fichiers modifiés

Et dans la sortie terminal, affiche aussi un résumé court avec :
- les principaux tests ajoutés
- la correction réalisée sur `ERROR`
- le statut global des tests