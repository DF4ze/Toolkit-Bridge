Nous passons à la Phase 1 de l’étape de réduction progressive de `TechnicalAdminFacade`.

Contexte de cette phase :
L’objectif n’est pas encore d’extraire des services, mais de sécuriser le comportement actuel avant refactoring.
Cette phase doit verrouiller les zones sensibles par des tests ciblés et corriger la logique fragile identifiée dans l’analyse.

Périmètre de la phase 1 :
- compléter les tests sur `listAgents()`
- compléter les tests sur `getOverview()`
- ajouter des tests de contrat sur `listRecentArtifacts()`
- analyser et corriger la logique fragile basée sur la string littérale `"ERROR"`
- ne pas encore extraire de nouveaux services
- ne pas modifier l’architecture globale à ce stade

Décisions d’architecture déjà actées :
- `TechnicalAdminFacade` est conservée comme façade délégante
- `getOverview()` reste dans la façade pour le moment
- le mapping DTO reste dans les futurs QueryServices
- la logique `agents / runtime / policy / tools` restera dans le périmètre admin
- cette phase est une phase de sécurisation avant extraction

Travail demandé :
Avant de coder, analyse cette phase dans le dépôt actuel.

Je veux que tu identifies précisément :

1. Les classes/fichiers concernés par cette phase
    - façade
    - tests existants
    - DTO concernés
    - éventuels enums/types d’événements/traces
    - classes utilisées par `getOverview()`, `listAgents()` et `listRecentArtifacts()`

2. L’état actuel de la couverture
    - ce qui est déjà testé
    - ce qui manque réellement
    - les comportements sensibles à verrouiller avant refactor

3. Le problème autour de `"ERROR"`
    - où se trouve exactement cette logique
    - quel type réel est manipulé
    - si un enum ou un type plus robuste existe déjà
    - quelle correction serait la plus saine et la plus minimale

4. Les risques techniques de cette phase
    - faux positifs de tests
    - tests trop couplés à l’implémentation
    - règles métier implicites non documentées
    - comportement ambigu sur les filtres d’artefacts
    - éventuels effets de bord sur dashboard / controller / pages admin

5. Un plan d’implémentation en 5 à 10 étapes maximum
    - seulement pour cette phase
    - ordonné
    - concret
    - sans partir sur les phases suivantes

Contraintes très importantes :
- N’implémente rien encore
- Ne commence pas l’extraction des services
- Ne change pas les contrôleurs sauf nécessité absolue identifiée
- Ne modifie pas les contrats DTO
- Ne propose pas de refonte large
- Cherche une sécurisation ciblée, propre et progressive

Livrable attendu :
Crée un rapport Markdown dans :
`data/rapport/v1.1/lot2/phase-1-analysis.md`

Le rapport doit contenir exactement les sections suivantes :

# Phase 1 — Analyse de sécurisation avant extraction

## 1. Fichiers et classes concernés

## 2. État actuel des tests existants

## 3. Comportements à verrouiller avant refactor

## 4. Analyse du point fragile autour de `ERROR`

## 5. Risques et points d’attention

## 6. Plan d’implémentation recommandé

## 7. Recommandation finale

Important :
À la fin, affiche aussi dans la sortie terminal un résumé court contenant :
- les principaux fichiers à toucher
- les 3 comportements les plus importants à verrouiller
- la correction recommandée pour le point `ERROR`