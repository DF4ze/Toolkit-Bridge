Nous passons maintenant à l’implémentation de la Phase 2.

Contexte :
L’analyse de phase 2 a confirmé que cette extraction est faisable à faible risque si elle reste strictement mécanique et progressive.
L’objectif est de sortir de `TechnicalAdminFacade` les trois blocs query à faible couplage, tout en conservant la façade comme point d’entrée public délégant.

Objectif de la phase 2 :
- extraire la partie tâches dans `AdminTaskQueryService`
- extraire la partie traces dans `TraceQueryService`
- extraire la partie artefacts dans `ArtifactQueryService`
- conserver `TechnicalAdminFacade` comme façade délégante
- conserver les signatures publiques actuelles
- ne pas modifier les contrôleurs ni les DTO
- laisser `getOverview()` dans la façade, mais le faire déléguer aux nouvelles méthodes via la façade

Travail demandé :
1. Relire rapidement l’analyse de phase 2 et vérifier si un micro-ajustement est nécessaire avant implémentation.
2. Si un ajustement mineur est nécessaire, l’appliquer de manière minimale et justifiée.
3. Implémenter ensuite entièrement la phase 2.

Implémentation attendue :

## A. Création des nouveaux services
Créer trois services Spring simples et ciblés :

### `AdminTaskQueryService`
Responsabilités :
- porter la logique aujourd’hui dans `listRecentTasks(...)`
- porter le mapping `toTaskItem(...)`

Dépendances attendues :
- `AdminTaskStore`
- `AdminTechnicalProperties`

### `TraceQueryService`
Responsabilités :
- porter la logique aujourd’hui dans `listRecentTraces(...)`
- porter le mapping `toTraceItem(...)`
- gérer localement le blank check nécessaire

Dépendances attendues :
- `AgentTraceQueryService`
- `AdminTechnicalProperties`

### `ArtifactQueryService`
Responsabilités :
- porter la logique aujourd’hui dans `listRecentArtifacts(...)`
- porter le mapping `toArtifactItem(...)`
- gérer localement le blank check nécessaire
- conserver strictement la règle de priorité :
    - `taskId` > `agentId` > global

Dépendances attendues :
- `ArtifactService`
- `AdminTechnicalProperties`

## B. Adaptation de `TechnicalAdminFacade`
Modifier `TechnicalAdminFacade` pour :
- injecter les trois nouveaux services
- conserver les méthodes publiques existantes :
    - `listRecentTasks(...)`
    - `listRecentTraces(...)`
    - `listRecentArtifacts(...)`
- transformer ces méthodes en simple délégation vers les nouveaux services
- ne pas modifier le comportement observable
- laisser `getOverview()` en place, en s’appuyant sur les méthodes publiques de la façade ou en déléguant proprement, sans changer son contrat

## C. Tests à adapter / ajouter
### Nouveaux tests à créer
Créer si pertinent :
- `AdminTaskQueryServiceTest`
- `TraceQueryServiceTest`
- `ArtifactQueryServiceTest`

Les nouveaux tests doivent couvrir la logique métier extraite :
- sanitation de limite
- mapping DTO
- branchements critiques
- tri / limit
- priorité des filtres artefacts

### Tests façade à ajuster
Adapter `TechnicalAdminFacadeTest` pour :
- déplacer les assertions métier spécifiques tâches/traces/artefacts vers les nouveaux services
- conserver côté façade :
    - tests de délégation
    - tests `getOverview()`
    - tests encore pertinents pour le contrat global

### Tests à conserver inchangés
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

## D. Contraintes importantes
- ne pas toucher aux contrôleurs
- ne pas modifier les DTO `TechnicalAdminView`
- ne pas toucher au bloc :
    - `listAgents()`
    - configuration
    - rétention
    - policy/runtime/tools
- ne pas extraire `getOverview()` dans un autre service
- ne pas introduire d’interface commune artificielle entre les 3 query services
- ne pas mutualiser prématurément les helpers
- accepter une légère duplication locale si elle garde le code plus simple et plus lisible

## E. Validation attendue
Exécuter les tests ciblés pertinents au minimum :
- `TechnicalAdminFacadeTest`
- `AdminTaskQueryServiceTest` si créé
- `TraceQueryServiceTest` si créé
- `ArtifactQueryServiceTest` si créé
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

Livrables attendus :
- code modifié
- nouveaux services créés
- tests créés ou adaptés
- résumé clair
- points d’attention éventuels
- liste des fichiers créés/modifiés

Important :
Créer aussi un rapport Markdown dans :
`data/rapport/v1.1/lot2/result_2_phase-2-implementation.md`

Le rapport doit contenir exactement :

# Phase 2 — Implémentation

## 1. Ajustements éventuels de l’analyse
## 2. Services créés
## 3. Modifications réalisées dans la façade
## 4. Tests ajoutés ou modifiés
## 5. Résultats d’exécution des tests
## 6. Points d’attention
## 7. Fichiers créés ou modifiés

Et dans la sortie terminal, afficher aussi un résumé court avec :
- les services créés
- ce qui a été retiré de la façade
- le statut global des tests