Nous passons maintenant à l’implémentation de la Phase 3 du lot 2.

Contexte :
Les phases précédentes du lot 2 ont été réalisées :
- phase 1 : sécurisation comportementale
- phase 2 : extraction des queries tâches / traces / artefacts

La façade `TechnicalAdminFacade` reste le point d’entrée public délégant.
Nous passons maintenant à l’extraction des lectures “pures” de rétention et de configuration.

Objectif de la phase 3 :
- extraire `listRetentionPolicies()` dans `RetentionQueryService`
- extraire `getConfigurationView()` dans `AdminConfigQueryService`
- conserver `TechnicalAdminFacade` comme façade délégante
- conserver strictement les contrats DTO et contrôleurs
- laisser `getOverview()` dans la façade, avec agrégation inchangée

Ordre d’extraction attendu :
1. `RetentionQueryService`
2. `AdminConfigQueryService`

Travail demandé :
1. Relire rapidement l’analyse de phase 3 (data/rapport/v1.1/lot2/result_3.phase-3-analysis.md) et vérifier si un micro-ajustement est nécessaire avant implémentation.
2. Si un ajustement mineur est nécessaire, l’appliquer de manière minimale et justifiée.
3. Implémenter ensuite entièrement la phase 3.

Implémentation attendue :

## A. Création de `RetentionQueryService`
Créer un service Spring simple dans `service.admin`.

Responsabilités :
- porter la logique aujourd’hui dans `listRetentionPolicies()`
- porter le mapping `RetentionPolicy -> TechnicalAdminView.RetentionItem`
- itérer sur `PersistableObjectFamily.values()`
- conserver exactement le comportement actuel

Dépendances attendues :
- `PersistenceRetentionPolicyResolver`

Contraintes :
- ne pas introduire de logique runtime
- ne pas modifier le contrat DTO
- ne pas changer l’ordre/présence des familles actuellement issues de `PersistableObjectFamily.values()`

## B. Création de `AdminConfigQueryService`
Créer un service Spring simple dans `service.admin`.

Responsabilités :
- porter la logique aujourd’hui dans `getConfigurationView()`
- porter le mapping `OpenAiLikeProperties -> TechnicalAdminView.LlmProviderItem`
- conserver la règle de sécurité :
    - ne jamais exposer la valeur brute de `apiKey`
    - exposer uniquement l’information booléenne `apiKeyConfigured`
- conserver exactement le rendu fonctionnel actuel

Dépendances attendues :
- `AgentDefinitionService`
- `AdministrableConfigurationGateway`

Contraintes :
- ne pas introduire de logique runtime/policy/tools
- garder un helper local `isBlank` si nécessaire
- ne pas modifier les DTO

## C. Adaptation de `TechnicalAdminFacade`
Modifier `TechnicalAdminFacade` pour :
- injecter `RetentionQueryService`
- injecter `AdminConfigQueryService`
- conserver les méthodes publiques existantes :
    - `listRetentionPolicies()`
    - `getConfigurationView()`
- transformer ces méthodes en simple délégation
- conserver `getOverview()` inchangée en structure
- faire en sorte que `getOverview()` continue d’agréger via les méthodes publiques de la façade ou une délégation propre, sans changement de contrat

Nettoyage attendu :
- retirer de la façade les dépendances/imports devenus inutiles après extraction
- laisser en place les helpers encore nécessaires au bloc agents/runtime/policy/tools
- retirer de la façade les mappings désormais déplacés

## D. Tests à ajouter / adapter
### Nouveaux tests recommandés
Créer :
- `RetentionQueryServiceTest`
- `AdminConfigQueryServiceTest`

Les tests doivent couvrir au minimum :

### `RetentionQueryServiceTest`
- itération sur les familles `PersistableObjectFamily.values()`
- projection correcte vers `TechnicalAdminView.RetentionItem`
- conservation des champs attendus (`family`, `ttl`, `disposition`)

### `AdminConfigQueryServiceTest`
- comptage des définitions d’agents
- comptage des providers
- mapping correct des providers vers `TechnicalAdminView.LlmProviderItem`
- non exposition de la clé API brute
- calcul correct de `apiKeyConfigured`

### Adaptation des tests façade
Adapter `TechnicalAdminFacadeTest` pour :
- injecter les nouveaux services
- transformer le test de configuration existant en test de délégation façade si nécessaire
- conserver les tests `getOverview*`
- ajuster les mocks pour refléter la délégation vers les nouveaux services

### Tests à conserver
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

## E. Contraintes importantes
- ne pas modifier les contrôleurs
- ne pas modifier les DTO `TechnicalAdminView`
- ne pas toucher au bloc agents/runtime/policy/tools
- ne pas toucher à la phase suivante
- ne pas introduire d’abstraction commune artificielle
- ne pas faire de refonte large
- garder une extraction mécanique, lisible, progressive

## F. Validation attendue
Exécuter les tests ciblés pertinents au minimum :
- `TechnicalAdminFacadeTest`
- `RetentionQueryServiceTest`
- `AdminConfigQueryServiceTest`
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`

Livrables attendus :
- code modifié
- nouveaux services créés
- tests créés ou adaptés
- résumé clair
- points d’attention éventuels
- liste des fichiers créés/modifiés

Important :
Créer aussi un rapport Markdown dans :
`data/rapport/v1.1/lot2/result_3.phase-3-implementation.md`

Le rapport doit contenir exactement :

# Phase 3 — Implémentation

## 1. Ajustements éventuels de l’analyse
## 2. Services créés
## 3. Modifications réalisées dans la façade
## 4. Tests ajoutés ou modifiés
## 5. Résultats d’exécution des tests
## 6. Points d’attention
## 7. Fichiers créés ou modifiés

Et dans la sortie terminal, afficher aussi un résumé court avec :
- les services créés
- ce qui a été retiré de la façade
- le statut global des tests