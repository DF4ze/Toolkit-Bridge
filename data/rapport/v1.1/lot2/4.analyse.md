Nous passons à la Phase 4 du lot 2.

Contexte :
Les phases précédentes du lot 2 sont terminées :
- phase 1 : sécurisation comportementale
- phase 2 : extraction tâches / traces / artefacts
- phase 3 : extraction configuration / rétention

`TechnicalAdminFacade` reste le point d’entrée public délégant.
Le principal bloc encore lourd dans la façade est désormais le bloc :
- agents
- runtime
- policy
- tools

C’est le périmètre de cette phase 4.

Objectif de la phase 4 :
Extraire de `TechnicalAdminFacade` le bloc le plus sensible et le plus couplé :
- `listAgents()`
- mappings associés
- résolution fallback policy/runtime/tools
- logique d’exposition des tools
- logique de projection runtime/policy

Le but est d’isoler ce bloc dans un service dédié, sans refonte métier profonde et sans changer les contrats publics.

Service cible pressenti :
- `AdminAgentQueryService`

Périmètre attendu :
- extraire la logique aujourd’hui portée par :
    - `listAgents()`
    - `toAgentItem(...)`
    - `resolvePolicyWithoutRuntime(...)`
    - `resolveExposedToolsFromPolicy(...)`
    - `toRuntimeItem(...)`
    - `toPolicyItem(...)`
    - `normalize(...)` si encore pertinent localement
- conserver `TechnicalAdminFacade` comme façade délégante
- ne pas modifier les contrôleurs
- ne pas modifier les DTO `TechnicalAdminView`
- ne pas extraire `getOverview()` dans cette phase
- ne pas toucher à l’architecture métier runtime/policy/tools au-delà de l’extraction admin

Travail demandé :
Avant de coder, analyse cette phase dans le dépôt actuel.

Je veux que tu identifies précisément :

1. Les fichiers/classes concernés
    - façade
    - services/registries utilisés par le bloc agents
    - modèles runtime/policy/tools manipulés
    - DTO manipulés
    - tests existants impactés
    - usages indirects via `getOverview()`

2. Le niveau réel de couplage du bloc à extraire
    - dépendances injectées
    - logique de fallback runtime absent
    - logique de résolution policy/tools
    - logique de mapping DTO
    - éventuelle logique transverse cachée
    - dépendances à de la configuration vs à du runtime

3. Les points sensibles d’extraction
    - risque de mélanger encore config et runtime
    - risque de déplacer une logique métier trop profonde
    - risque de casser `getOverview()`
    - risque sur les tests existants
    - risque de duplication ou de helpers mal placés
    - ce qui doit impérativement rester inchangé

4. Le meilleur plan d’extraction progressif
    - ce qui doit être déplacé exactement
    - ce qui doit rester temporairement dans la façade
    - si `normalize(...)` doit migrer ou rester
    - si certains helpers doivent rester locaux au futur service
    - comment garder une extraction purement admin sans refonte du domaine

5. L’impact probable sur les tests
    - tests façade à adapter
    - tests à déplacer vers le futur service
    - nouveaux tests à créer
    - tests à conserver tels quels

Contraintes importantes :
- N’implémente rien encore
- Ne commence pas le nettoyage final
- Ne modifie pas les contrôleurs
- Ne modifie pas les DTO `TechnicalAdminView`
- Ne propose pas une refonte large du domaine runtime/policy/tools
- Ne déplace pas `getOverview()` vers un autre service
- Cherche une extraction progressive, lisible, maîtrisée, sans sur-ingénierie

Livrable attendu :
Créer un rapport Markdown dans :
`data/rapport/v1.1/lot2/result_4.phase-4-analysis.md`

Le rapport doit contenir exactement les sections suivantes :

# Phase 4 — Analyse d’extraction du bloc agents runtime policy tools

## 1. Fichiers et classes concernés

## 2. Analyse du bloc agents runtime policy tools

## 3. Points sensibles et risques d’extraction

## 4. Plan d’extraction recommandé

## 5. Impact sur les tests

## 6. Recommandation finale

Important :
À la fin, affiche aussi dans la sortie terminal un résumé court contenant :
- les principales dépendances du futur `AdminAgentQueryService`
- les risques principaux à surveiller
- le plan recommandé d’extraction