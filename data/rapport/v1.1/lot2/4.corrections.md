Nous faisons un dernier tour de finition sur la Phase 4 du lot 2.

Contexte :
La phase 4 est globalement réussie :
- extraction de `AdminAgentQueryService`
- façade `TechnicalAdminFacade` conservée comme point d’entrée délégant
- `getOverview()` conservée dans la façade
- DTO et contrôleurs inchangés
- tests ciblés passants

La relecture ne remonte aucun problème bloquant, seulement quelques renforcements utiles avant clôture complète du lot 2.

Objectif :
- réduire le risque de dérive entre fallback admin et logique runtime
- renforcer un test de mapping complet
- documenter localement une contrainte d’alignement
- ne pas changer le comportement fonctionnel
- ne pas élargir le périmètre

Corrections demandées :

## 1. Ajouter un verrou de parité explicite
Dans `AdminAgentQueryServiceTest`, ajouter un test ciblé qui renforce explicitement la parité attendue entre :
- la logique de fallback admin dans `resolvePolicyWithoutRuntime(...)`
- et les attentes préparées côté runtime/factory

But :
- réduire le risque de divergence future
- sans refondre ni factoriser maintenant la logique partagée

Contrainte :
- ne pas modifier `AgentRuntimeFactory`
- ne pas introduire d’abstraction commune nouvelle
- rester sur un test de verrouillage comportemental

## 2. Renforcer un test de mapping principal complet
Dans `AdminAgentQueryServiceTest`, ajouter ou compléter un test unique qui verrouille plus explicitement les champs principaux de `AgentItem`, par exemple :
- provider
- model
- orchestrator
- policyName
- toolsEnabled
- runtime
- policy
- exposedTools

But :
- mieux figer le mapping complet
- garder la couverture lisible sans multiplier les tests redondants

## 3. Documenter l’alignement attendu avec la factory runtime
Ajouter un commentaire technique court dans `resolvePolicyWithoutRuntime(...)` indiquant que :
- cette logique de fallback admin doit rester alignée avec la logique de préparation runtime
- elle existe pour la projection admin en absence de runtime
- aucune refonte n’est attendue dans cette phase

Contrainte :
- commentaire bref
- pas de documentation verbeuse
- pas de déplacement de logique

Important :
- ne pas modifier les DTO
- ne pas modifier les contrôleurs
- ne pas toucher à `getOverview()`
- ne pas changer le comportement fonctionnel
- ne pas lancer une nouvelle phase
- ne pas introduire de refonte domaine runtime/policy/tools

Validation attendue :
Relancer les tests ciblés pertinents au minimum :
- `AdminAgentQueryServiceTest`
- `TechnicalAdminFacadeTest`
- `TechnicalAdminControllerTest`
- `TechnicalAdminPageControllerTest`
- `AdminDashboardServiceTest`
- `ToolkitBridgeApplicationTests`

Livrables attendus :
- code ajusté
- tests renforcés
- mini-résumé
- liste des fichiers modifiés

Important :
Créer aussi un rapport Markdown dans :
`data/rapport/v1.1/lot2/result_4.phase-4-corrections.md`

Le rapport doit contenir exactement :

# Phase 4 — Corrections finales

## 1. Renforts apportés sur les tests
## 2. Verrouillage de parité fallback/runtime
## 3. Documentation locale ajoutée
## 4. Résultats des tests
## 5. Fichiers modifiés

Et dans la sortie terminal, afficher aussi un résumé court avec :
- les tests renforcés
- le verrouillage ajouté
- le statut final des tests