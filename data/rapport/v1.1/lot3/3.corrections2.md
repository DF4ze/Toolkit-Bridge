# Prompt Codex — Correction complémentaire Phase 3 — Normalisation agentId

## 🎯 Objectif

Aligner la gestion de `agentId` dans les traces critiques
avec la stratégie déjà mise en place en Phase 2 (tâches admin),
afin d’assurer un comportement cohérent et prédictible.

---

## ⚠️ Contraintes

- ❌ Pas de refactor global
- ❌ Pas de modification des contrats publics
- ❌ Pas de changement de structure DB
- ❌ Pas d’ajout de nouveaux champs

- ✅ Correction ciblée
- ✅ Cohérence write/read
- ✅ Impact minimal

---

## 🧠 Contexte

Actuellement :
- `agentId` est extrait des événements (`AgentTraceEvent.correlation`)
- la normalisation côté lecture (`TraceQueryService`) est limitée (trim uniquement)

Dans Phase 2 :
- les identifiants techniques sont normalisés (lower-case)

Objectif :
👉 uniformiser le comportement pour éviter incohérences de filtrage

---

## 🛠️ Travail à réaliser

### 1. Normalisation à l’écriture

Dans le mapper persistence (ex: `CriticalAgentTraceMapper`) :

- normaliser `agentId` avant stockage :
    - trim
    - toLowerCase
- gérer les cas null / vide :
    - conserver null si absent

---

### 2. Normalisation à la lecture

Dans `TraceQueryService` :

- appliquer la même normalisation sur le paramètre `agentId` en entrée :
    - trim
    - toLowerCase

👉 garantir cohérence entre :
- valeur stockée
- valeur filtrée

---

### 3. Documentation du comportement

Ajouter un commentaire clair dans le code :

```text
agentId is a technical identifier, normalized to lower-case.
It is not intended for UI display.
```

---
### 4. Rapport d'execution

Ajouter un rapport d'execution en fin de fichier `result_03_correction.md` :